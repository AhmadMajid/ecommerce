<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background-color: #f8f9fa;
        padding: 20px;
        border-radius: 5px;
        margin-bottom: 20px;
      }
      .original-message {
        background-color: #f1f3f4;
        border-left: 4px solid #ccc;
        padding: 15px;
        margin-top: 30px;
      }
      .original-message h4 {
        margin-top: 0;
        color: #666;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h2>Reply from <%= Rails.application.class.module_parent_name %></h2>
    </div>

    <div class="reply-content">
      <%= simple_format(@reply_content) %>
    </div>

    <div class="original-message">
      <h4>Original message:</h4>
      <p><strong>From:</strong> <%= @contact_message.name %></p>
      <p><strong>Subject:</strong> <%= @contact_message.subject %></p>
      <p><strong>Date:</strong> <%= @contact_message.created_at.strftime("%B %d, %Y at %I:%M %p") %></p>
      <div>
        <%= simple_format(@contact_message.message) %>
      </div>
    </div>

    <hr style="margin: 30px 0; border: none; border-top: 1px solid #ddd;">
    <p style="color: #666; font-size: 12px;">
      This email was sent in response to your message submitted through our website contact form.
    </p>
  </body>
</html>
