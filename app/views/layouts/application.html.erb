<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for?(:title) ? yield(:title) + " - " : "" %>StyleMart</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "StyleMart - Your premier destination for modern fashion and lifestyle products" %>">
    <meta name="keywords" content="fashion, ecommerce, online store, shopping, products, style">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <!-- Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico">

    <style>
      body { font-family: 'Inter', sans-serif; }
    </style>
  </head>

  <body class="bg-gray-50 antialiased">
    <!-- Top Banner -->
    <div class="bg-gradient-to-r from-purple-600 to-blue-600 text-white text-center py-2 text-sm">
      <div class="container mx-auto px-4">
        <i class="fas fa-truck mr-2"></i>Free shipping on orders over $50!
        <button class="ml-4 text-white/80 hover:text-white">
          <i class="fas fa-times"></i>
        </button>
      </div>
    </div>

    <!-- Main Navigation -->
    <nav class="bg-white shadow-lg sticky top-0 z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <!-- Logo -->
          <div class="flex-shrink-0">
            <%= link_to root_path, class: "flex items-center space-x-3 group" do %>
              <div class="w-10 h-10 bg-gradient-to-br from-purple-600 to-blue-600 rounded-xl flex items-center justify-center group-hover:scale-105 transition-transform duration-200">
                <i class="fas fa-shopping-bag text-white text-lg"></i>
              </div>
              <span class="text-2xl font-bold bg-gradient-to-r from-purple-600 to-blue-600 bg-clip-text text-transparent">
                StyleMart
              </span>
            <% end %>
          </div>

          <!-- Main Navigation Links -->
          <div class="hidden md:flex items-center space-x-8">
            <%= link_to "Home", root_path,
                class: "text-gray-700 hover:text-purple-600 font-medium transition-colors duration-200 #{current_page?(root_path) ? 'text-purple-600 border-b-2 border-purple-600 pb-1' : ''}" %>
            <%= link_to "Products", products_path,
                class: "text-gray-700 hover:text-purple-600 font-medium transition-colors duration-200 #{current_page?(products_path) ? 'text-purple-600 border-b-2 border-purple-600 pb-1' : ''}" %>
            <%= link_to "Categories", categories_path,
                class: "text-gray-700 hover:text-purple-600 font-medium transition-colors duration-200 #{current_page?(categories_path) ? 'text-purple-600 border-b-2 border-purple-600 pb-1' : ''}" %>
            <a href="#" class="text-gray-700 hover:text-purple-600 font-medium transition-colors duration-200">About</a>
            <a href="#" class="text-gray-700 hover:text-purple-600 font-medium transition-colors duration-200">Contact</a>
          </div>

          <!-- Search & Actions -->
          <div class="flex items-center space-x-4">
            <!-- Search (Desktop) -->
            <div class="hidden lg:block relative">
              <form action="<%= products_path %>" method="get" class="relative">
                <input type="text"
                       name="search"
                       placeholder="Search products..."
                       value="<%= params[:search] %>"
                       class="w-64 pl-10 pr-4 py-2 border border-gray-300 rounded-full focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all duration-200">
                <button type="submit" class="absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400 hover:text-gray-600">
                  <i class="fas fa-search"></i>
                </button>
              </form>
            </div>

            <!-- Cart -->
            <div class="relative" data-controller="cart-dropdown">
              <button data-action="click->cart-dropdown#toggle"
                      class="relative p-2 text-gray-600 hover:text-purple-600 transition-colors duration-200">
                <i class="fas fa-shopping-cart text-xl"></i>
                <span id="cart-count"
                      class="absolute -top-1 -right-1 bg-purple-600 text-white text-xs rounded-full h-5 w-5 flex items-center justify-center font-medium <%= current_cart.items.sum(:quantity) > 0 ? '' : 'hidden' %>">
                  <%= current_cart.items.sum(:quantity) %>
                </span>
              </button>

              <!-- Cart Dropdown -->
              <div data-cart-dropdown-target="dropdown"
                   class="hidden absolute right-0 mt-2 w-80 bg-white rounded-lg shadow-xl border border-gray-200 z-50">
                <div class="p-4 border-b border-gray-200">
                  <h3 class="font-semibold text-gray-900">Shopping Cart</h3>
                </div>
                <div id="cart-items" class="max-h-64 overflow-y-auto">
                  <% if current_cart.items.any? %>
                    <% current_cart.items.includes(:product).each do |item| %>
                      <div class="p-4 border-b border-gray-100 flex items-center space-x-3">
                        <div class="flex-shrink-0 w-12 h-12 bg-gray-200 rounded-lg flex items-center justify-center">
                          <% if item.product.images.any? %>
                            <%= image_tag item.product.images.first, class: "w-full h-full object-cover rounded-lg" %>
                          <% else %>
                            <i class="fas fa-image text-gray-400"></i>
                          <% end %>
                        </div>
                        <div class="flex-1">
                          <h4 class="font-medium text-sm text-gray-900"><%= truncate(item.product.name, length: 30) %></h4>
                          <p class="text-sm text-gray-500">Qty: <%= item.quantity %> × $<%= item.product.price %></p>
                        </div>
                        <button data-action="click->cart#removeItem"
                                data-item-id="<%= item.id %>"
                                class="text-red-400 hover:text-red-600 transition-colors">
                          <i class="fas fa-trash text-sm"></i>
                        </button>
                      </div>
                    <% end %>
                  <% else %>
                    <div class="p-8 text-center text-gray-500">
                      <i class="fas fa-shopping-cart text-3xl mb-2"></i>
                      <p>Your cart is empty</p>
                    </div>
                  <% end %>
                </div>
                <% if current_cart.items.any? %>
                  <div class="p-4 border-t border-gray-200">
                    <div class="flex justify-between items-center mb-3">
                      <span class="font-semibold">Total:</span>
                      <span class="font-bold text-lg">$<%= current_cart.total %></span>
                    </div>
                    <div class="space-y-2">
                      <%= link_to "View Cart", cart_path,
                          class: "w-full bg-gray-100 text-gray-700 py-2 px-4 rounded-lg font-medium hover:bg-gray-200 transition-colors text-center block" %>
                      <%= link_to "Checkout", new_checkout_path,
                          class: "w-full bg-gradient-to-r from-purple-600 to-blue-600 text-white py-2 px-4 rounded-lg font-medium hover:from-purple-700 hover:to-blue-700 transition-all text-center block" %>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>

            <!-- User Menu -->
            <% if user_signed_in? %>
              <div class="relative" data-controller="user-dropdown">
                <button data-action="click->user-dropdown#toggle"
                        class="flex items-center space-x-2 text-gray-700 hover:text-purple-600 transition-colors duration-200">
                  <div class="w-8 h-8 bg-gradient-to-br from-purple-600 to-blue-600 rounded-full flex items-center justify-center">
                    <span class="text-white font-medium text-sm">
                      <%= current_user.first_name&.first&.upcase || current_user.email.first.upcase %>
                    </span>
                  </div>
                  <span class="hidden md:block font-medium">
                    <%= current_user.first_name || "Account" %>
                  </span>
                  <i class="fas fa-chevron-down text-xs"></i>
                </button>

                <!-- User Dropdown -->
                <div data-user-dropdown-target="dropdown"
                     class="hidden absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-xl border border-gray-200 z-50">
                  <div class="p-2">
                    <%= link_to "#", class: "flex items-center space-x-2 px-3 py-2 text-gray-700 hover:bg-gray-100 rounded-lg transition-colors" do %>
                      <i class="fas fa-user text-gray-400"></i>
                      <span>Profile</span>
                    <% end %>
                    <%= link_to "#", class: "flex items-center space-x-2 px-3 py-2 text-gray-700 hover:bg-gray-100 rounded-lg transition-colors" do %>
                      <i class="fas fa-box text-gray-400"></i>
                      <span>Orders</span>
                    <% end %>
                    <%= link_to "#", class: "flex items-center space-x-2 px-3 py-2 text-gray-700 hover:bg-gray-100 rounded-lg transition-colors" do %>
                      <i class="fas fa-heart text-gray-400"></i>
                      <span>Wishlist</span>
                    <% end %>
                    <hr class="my-2">
                    <%= link_to destroy_user_session_path, method: :delete,
                        class: "flex items-center space-x-2 px-3 py-2 text-red-600 hover:bg-red-50 rounded-lg transition-colors" do %>
                      <i class="fas fa-sign-out-alt text-red-400"></i>
                      <span>Sign Out</span>
                    <% end %>
                  </div>
                </div>
              </div>
            <% else %>
              <div class="flex items-center space-x-3">
                <%= link_to "Sign In", new_user_session_path,
                    class: "text-gray-700 hover:text-purple-600 font-medium transition-colors duration-200" %>
                <%= link_to "Sign Up", new_user_registration_path,
                    class: "bg-gradient-to-r from-purple-600 to-blue-600 text-white px-4 py-2 rounded-lg font-medium hover:from-purple-700 hover:to-blue-700 transition-all shadow-md hover:shadow-lg" %>
              </div>
            <% end %>

            <!-- Mobile Menu Button -->
            <button class="md:hidden p-2 text-gray-600 hover:text-purple-600" data-controller="mobile-menu" data-action="click->mobile-menu#toggle">
              <i class="fas fa-bars text-xl"></i>
            </button>
          </div>
        </div>
      </div>

      <!-- Mobile Menu -->
      <div data-mobile-menu-target="menu" class="hidden md:hidden border-t border-gray-200 bg-white">
        <div class="px-4 py-4 space-y-2">
          <!-- Mobile Search -->
          <form action="<%= products_path %>" method="get" class="relative mb-4">
            <input type="text"
                   name="search"
                   placeholder="Search products..."
                   value="<%= params[:search] %>"
                   class="w-full pl-10 pr-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent">
            <button type="submit" class="absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400">
              <i class="fas fa-search"></i>
            </button>
          </form>

          <%= link_to "Home", root_path, class: "block px-3 py-2 text-gray-700 hover:text-purple-600 hover:bg-purple-50 rounded-lg transition-colors font-medium" %>
          <%= link_to "Products", products_path, class: "block px-3 py-2 text-gray-700 hover:text-purple-600 hover:bg-purple-50 rounded-lg transition-colors font-medium" %>
          <%= link_to "Categories", categories_path, class: "block px-3 py-2 text-gray-700 hover:text-purple-600 hover:bg-purple-50 rounded-lg transition-colors font-medium" %>
          <a href="#" class="block px-3 py-2 text-gray-700 hover:text-purple-600 hover:bg-purple-50 rounded-lg transition-colors font-medium">About</a>
          <a href="#" class="block px-3 py-2 text-gray-700 hover:text-purple-600 hover:bg-purple-50 rounded-lg transition-colors font-medium">Contact</a>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice || alert %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <% if notice %>
          <div class="bg-green-50 border-l-4 border-green-400 p-4 mb-4" role="alert">
            <div class="flex">
              <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-green-400" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                </svg>
              </div>
              <div class="ml-3">
                <p class="text-sm text-green-700 font-medium">
                  <%= notice %>
                </p>
              </div>
              <div class="ml-auto pl-3">
                <div class="-mx-1.5 -my-1.5">
                  <button type="button" class="inline-flex rounded-md p-1.5 text-green-500 hover:bg-green-100 focus:outline-none" onclick="this.parentElement.parentElement.parentElement.parentElement.remove()">
                    <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                      <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd" />
                    </svg>
                  </button>
                </div>
              </div>
            </div>
          </div>
        <% end %>
        <% if alert %>
          <div class="bg-red-50 border-l-4 border-red-400 p-4 mb-4" role="alert">
            <div class="flex">
              <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                </svg>
              </div>
              <div class="ml-3">
                <p class="text-sm text-red-700 font-medium">
                  <%= alert %>
                </p>
              </div>
              <div class="ml-auto pl-3">
                <div class="-mx-1.5 -my-1.5">
                  <button type="button" class="inline-flex rounded-md p-1.5 text-red-500 hover:bg-red-100 focus:outline-none" onclick="this.parentElement.parentElement.parentElement.parentElement.remove()">
                    <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                      <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd" />
                    </svg>
                  </button>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="flex-1">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-900 text-white mt-auto">
      <div class="max-w-7xl mx-auto py-12 px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
          <!-- Company Info -->
          <div class="md:col-span-2">
            <div class="flex items-center space-x-2 mb-4">
              <div class="w-8 h-8 bg-purple-600 rounded-lg flex items-center justify-center">
                <i class="fas fa-shopping-bag text-white text-lg"></i>
              </div>
              <h3 class="text-xl font-bold">StyleMart</h3>
            </div>
            <p class="text-gray-400 mb-4 max-w-md">
              Your premier destination for quality products at competitive prices.
              We're committed to providing exceptional customer service and fast shipping.
            </p>
            <!-- Newsletter Signup -->
            <div class="mb-6">
              <h4 class="text-sm font-semibold mb-2">Subscribe to our newsletter</h4>
              <div class="flex max-w-md">
                <input type="email" placeholder="Enter your email"
                       class="flex-1 px-4 py-2 bg-gray-800 border border-gray-700 rounded-l-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500 text-white placeholder-gray-400">
                <button class="px-4 py-2 bg-purple-600 hover:bg-purple-700 rounded-r-lg font-medium transition-colors">
                  Subscribe
                </button>
              </div>
            </div>
            <!-- Social Media Icons -->
            <div class="flex space-x-4">
              <a href="#" class="text-gray-400 hover:text-white transition-colors">
                <i class="fab fa-facebook text-xl"></i>
              </a>
              <a href="#" class="text-gray-400 hover:text-white transition-colors">
                <i class="fab fa-twitter text-xl"></i>
              </a>
              <a href="#" class="text-gray-400 hover:text-white transition-colors">
                <i class="fab fa-instagram text-xl"></i>
              </a>
              <a href="#" class="text-gray-400 hover:text-white transition-colors">
                <i class="fab fa-linkedin text-xl"></i>
              </a>
            </div>
          </div>
          <!-- Quick Links -->
          <div>
            <h3 class="text-lg font-semibold mb-4">Quick Links</h3>
            <ul class="space-y-3">
              <li><a href="#" class="text-gray-400 hover:text-white transition-colors">About Us</a></li>
              <li><a href="#" class="text-gray-400 hover:text-white transition-colors">Contact</a></li>
              <li><a href="#" class="text-gray-400 hover:text-white transition-colors">Shipping Info</a></li>
              <li><a href="#" class="text-gray-400 hover:text-white transition-colors">Returns & Exchanges</a></li>
              <li><a href="#" class="text-gray-400 hover:text-white transition-colors">Size Guide</a></li>
              <li><a href="#" class="text-gray-400 hover:text-white transition-colors">FAQ</a></li>
            </ul>
          </div>
          <!-- Customer Service -->
          <div>
            <h3 class="text-lg font-semibold mb-4">Customer Service</h3>
            <ul class="space-y-3">
              <li><a href="#" class="text-gray-400 hover:text-white transition-colors">Privacy Policy</a></li>
              <li><a href="#" class="text-gray-400 hover:text-white transition-colors">Terms of Service</a></li>
              <li><a href="#" class="text-gray-400 hover:text-white transition-colors">Track Your Order</a></li>
              <li><a href="#" class="text-gray-400 hover:text-white transition-colors">Support Center</a></li>
              <li><a href="#" class="text-gray-400 hover:text-white transition-colors">Wholesale</a></li>
              <li><a href="#" class="text-gray-400 hover:text-white transition-colors">Gift Cards</a></li>
            </ul>
          </div>
        </div>
        <!-- Bottom Footer -->
        <div class="border-t border-gray-800 mt-8 pt-8">
          <div class="flex flex-col md:flex-row justify-between items-center">
            <p class="text-gray-400 text-sm">
              &copy; <%= Date.current.year %> StyleMart. All rights reserved.
            </p>
            <!-- Payment Methods -->
            <div class="flex items-center space-x-4 mt-4 md:mt-0">
              <span class="text-gray-400 text-sm">We accept:</span>
              <div class="flex space-x-2">
                <!-- Visa -->
                <div class="w-10 h-6 bg-white rounded flex items-center justify-center">
                  <span class="text-blue-600 text-xs font-bold">VISA</span>
                </div>
                <!-- Mastercard -->
                <div class="w-10 h-6 bg-white rounded flex items-center justify-center">
                  <span class="text-red-600 text-xs font-bold">MC</span>
                </div>
                <!-- PayPal -->
                <div class="w-10 h-6 bg-blue-600 rounded flex items-center justify-center">
                  <span class="text-white text-xs font-bold">PP</span>
                </div>
                <!-- Apple Pay -->
                <div class="w-10 h-6 bg-black rounded flex items-center justify-center">
                  <span class="text-white text-xs">🍎</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </footer>

    <!-- JavaScript for Mobile Menu and Dropdowns -->
    <script>
      // Auto-hide flash messages after 5 seconds
      setTimeout(function() {
        const flashMessages = document.querySelectorAll('[role="alert"]');
        flashMessages.forEach(function(message) {
          message.style.transition = 'opacity 0.5s';
          message.style.opacity = '0';
          setTimeout(function() {
            message.remove();
          }, 500);
        });
      }, 5000);
    </script>
  </body>
</html>
